import { AttributesFormFields } from './types';

export const attributesFormFields: AttributesFormFields = {
  name: 'name',
  isActive: 'isActive',
  sortOrder: 'sortOrder',
};

export const TABLE_VARIANTS_ID = 'variantsTable';
